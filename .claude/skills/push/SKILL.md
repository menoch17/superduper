# Push to Remote
1. Run `pwd` and `git remote -v` to confirm correct repo
2. Run `git status` and `git log --oneline -5` to check state
3. Run `git fetch origin` to check for divergence
4. If branches diverged, ask user: merge, rebase, or force push?
5. Push and confirm success
